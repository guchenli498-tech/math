# 数据获取快速开始指南

## 🚀 最快方案（30分钟内开始）

### 步骤1: 下载311老鼠投诉数据（最重要，10分钟）

**方法A - 使用脚本（推荐）**：
```bash
python download_311_data.py
```

**方法B - 手动下载**：
1. 打开：https://data.cityofnewyork.us/Social-Services/311-Service-Requests-from-2010-to-Present/erm2-nwe9
2. 点击 "Filter"
3. 设置：`complaint_type` 包含 "Rodent"，`borough` = "MANHATTAN"
4. 点击 "Export" → "CSV"
5. 保存为 `311_rodent_manhattan.csv`

---

### 步骤2: 下载人口普查数据（10分钟）

**方法A - 使用脚本**：
```bash
python download_census_data.py
```

**方法B - 手动下载**：
1. 打开：https://data.cityofnewyork.us/City-Government/Census-Demographics-at-the-Neighborhood-Tabulation-Area/37yn-6x8y
2. 点击 "Export" → "CSV"
3. 保存为 `census_nta_manhattan.csv`

---

### 步骤3: 分析现有DSNY数据（10分钟）

```bash
python analyze_data_fast.py
```

或手动查看CSV文件，提取MN01-MN12区域的信息。

---

## 📋 完整数据清单

### 必需数据（高优先级）

| 数据 | 来源 | 获取方式 | 时间 |
|------|------|----------|------|
| ✅ DSNY区域地理 | 已有 | - | - |
| ⚠️ 311老鼠投诉 | NYC Open Data | 脚本/网页 | 10分钟 |
| ⚠️ 人口普查 | NYC Open Data | 脚本/网页 | 10分钟 |
| ⚠️ 垃圾产生量 | 估算/DSNY报告 | 估算 | 30分钟 |
| ⚠️ 建筑数据 | NYC Open Data | 网页 | 15分钟 |

### 可选数据（中优先级）

| 数据 | 来源 | 获取方式 | 时间 |
|------|------|----------|------|
| 街道网络 | OpenStreetMap | osmnx库 | 30分钟 |
| 当前收集频率 | 估算 | 假设 | 10分钟 |

---

## 🛠️ 工具准备

### 安装Python库

```bash
pip install pandas requests geopandas shapely
```

### 如果需要街道网络数据

```bash
pip install osmnx networkx
```

---

## ⚡ 如果数据获取失败

### 使用估算数据开始建模

创建文件 `estimated_data.py`：

```python
import pandas as pd
import numpy as np

# 基于问题描述的估算
# 曼哈顿12个区域
districts = [f"MN{i:02d}" for i in range(1, 13)]

# 估算数据
estimated_data = {
    'district': districts,
    # 基于面积估算人口（假设密度）
    'population': np.random.randint(50000, 150000, 12),
    # 每人每天3.5磅垃圾
    'daily_waste_lbs': None,  # 将在下面计算
    # 当前收集频率（假设）
    'current_pickups_per_week': [2] * 12,
    # 老鼠投诉（基于垃圾暴露时间估算）
    'rodent_complaints_per_year': None,  # 将在下面计算
}

# 计算垃圾产生量
estimated_data['daily_waste_lbs'] = estimated_data['population'] * 3.5
estimated_data['daily_waste_tons'] = estimated_data['daily_waste_lbs'] / 2000

# 估算老鼠投诉（假设与垃圾暴露时间相关）
estimated_data['rodent_complaints_per_year'] = (
    estimated_data['daily_waste_tons'] * 10 + 
    np.random.randint(-50, 50, 12)
).astype(int)

df = pd.DataFrame(estimated_data)
df.to_csv('estimated_district_data.csv', index=False)
print("已创建估算数据: estimated_district_data.csv")
```

**在报告中说明**：这些是估算值，基于问题描述和合理假设。

---

## 📝 数据获取检查清单

- [ ] 311老鼠投诉数据已下载
- [ ] 人口普查数据已下载
- [ ] DSNY区域数据已分析
- [ ] 数据已清洗（处理缺失值、异常值）
- [ ] 数据已映射到DSNY区域（MN01-MN12）
- [ ] 创建了区域特征矩阵

---

## 🎯 下一步

数据获取后：
1. 数据预处理和清洗
2. 将不同数据源映射到DSNY区域
3. 创建区域特征矩阵
4. 开始建立优化模型

---

**记住**：不要因为数据获取而耽误建模。可以先使用估算数据建立模型框架！

